<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title><%= title %></title>
    <link rel="shortcut icon" href="/css/favicon.ico"/>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/css/bootstrap.min.css" />
    <link rel="stylesheet" href="/css/bootstrap-rtl.min.css" />    
    <link rel="stylesheet" href="/css/vazir.css">
    <link rel="stylesheet" href="/css/home.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/font-awesome.css">
    <style>
        #particles-js{
            position: fixed;
            width: 100%;
            height: 100%;
            z-index: 0;
            top: 100px;
            left: 0px;
        }
    </style>
</head>
<body>
    
    <nav class="navbar navbar-expand-md navbar-dark bg-dark">
        <div class="container">
          <% if(auth.check) { %>
            <a class="navbar-brand" href="/"><%= auth.user.username %></a>
            <% } else{ %>
              <a class="navbar-brand btn btn-success" href="/">سامانه ثبت اعتراضات</a>
          <% } %>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExampleDefault" aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarsExampleDefault">
              <ul class="navbar-nav ml-auto"></ul>
              <ul class="nav navbar-nav navbar-right">
                    <% if(auth.check) { %>
                      <li><a class="btn btn-outline-secondary btn-sm ml-2" href="/logout">خروج از سامانه</a></li>
                   <% } else if(path != '/register') {%>
                    <li><a class="btn btn-outline-danger btn-sm" href="/register">عضویت</a></li>
                    <% } else if(path == '/register') { %>
                    <li><a class="btn btn-outline-success btn-sm" href="/">ورود</a></li>
                    <% } %>
              </ul>
            </div>
        </div>
      </nav>